# Dead-Code Sweep — 20260621

**Branch:** `chore/dead-code-sweep-20260621`  
**Date:** 2026-06-21  
**Author:** AI sweep agent  
**Reviewed by:** Yaseen (pending device smoke-test)

---

## Summary

| Metric | Value |
|--------|-------|
| Files changed (vs `origin/main`) | 115 |
| Lines deleted | 9,463 |
| Lines added | 416 |
| Whole files deleted | 38 |
| Commits on branch | 6 |
| `tsc --noEmit` (final) | exit 0 |
| `npm test` (final) | 16/16 files, 91/91 tests |
| `npm run lint` (final) | exit 0 |

---

## Phase Log

### Phase 0 — Baseline & inventory
- Branch `chore/dead-code-sweep-20260621` cut from `origin/main`.
- Baselines captured in `docs/audits/dead-code-sweep-20260621/`.
- `tsc --noEmit`: exit 0.
- `npm test`: 16/16 files, 91/91 passing.
- `knip`: inventoried ~120+ findings; bucketed into phases.

### Phase 1 — Delete provably-unused whole files
**Commit:** `32f2db0`  
35 files deleted, all confirmed by `knip` + `rg` zero-references + not under `app/`.

**Deleted:**
- `components/home/`: HomeHeader, HomeHighlightRow, HomeSection, RunningChallengesSection, StreakHeroV2, StreakRiskBanner (component only — type preserved), HomeProgressBanner, HomeMoodSelector, HomeMoodSelectorV2
- `components/profile/`: MiniStats, ProfileChallengeCard, ProfileHeader (legacy), FriendsCard
- `components/typography/`: Body, BodySm, Caption, Display, Headline, SubHeadline, XS (entire directory)
- `components/discover/`: DiscoverHeader, DiscoverCard, DiscoverChallengeCard, InviteCard, StreakRiskBanner (component body), SocialProofBar, ActivitySummaryCard, MoodInsightCard, FriendActivityCard
- `components/task/`: TaskCard, TaskCardLegacy, TaskProgressCard
- `lib/`: formatters.ts, validation.ts

### Phase 2 — Unused exports & internal dead symbols
**Commit:** `d0c499a`  
81 dead exports removed; 74 unused type exports removed. Cascade: 3 additional files deleted, several files rewritten to type-only.

**Key changes:**
- `components/skeletons/`: Removed re-exports for `SkeletonChallengeCard`, `SkeletonHeroCard`, `SkeletonHomeChallengeCard`; those 3 files deleted.
- `components/ui/index.ts`: Removed re-exports for `PrimaryButton`, `EmptyState`.
- `components/challenges/HeroFeaturedCard.tsx`: Rewritten to type-only (kept `HeroFeaturedData`, `HeroFeaturedDifficulty`, `HeroFeaturedProofType`).
- `components/discover/StreakRiskBanner.tsx`: Rewritten to type-only (kept `StreakAtRiskData`).
- `components/TaskEditorModal.tsx`: Rewritten from 1,745-line dead component to 82-line type-only module (kept `TaskEditorTask`).
- `lib/design-system.ts`: Deleted `DS_TOUCH`, `DS_BUTTON`; unexported `CHALLENGE_CATEGORY_COLORS`, `DEFAULT_CATEGORY_COLOR`.
- `lib/notifications.ts`: Deleted `formatTimeForNotification`, `setupNotificationChannel`, `cancelChallengeReminders`; unexported constants.
- `lib/profile-display.ts`: Deleted `profileHandleAt`; made `isAutoGeneratedUsername` private.
- `lib/sanitize.ts`: Made `stripHtml`, `MAX_LENGTHS`, `sanitizeText` private (internal helpers).
- `types/index.ts`: Pruned from 438 → 217 lines; removed `export` from 33 dead domain model types.
- 29 files: Removed redundant `export default X;` statements where named export was the only consumer.
- Fix: `components/onboarding/v2/screens/CommitmentScreen.tsx` — Changed `import StreakFlame from ...` → `import { StreakFlame } from ...` (default export removed in cleanup).

### Phase 3 — Duplicate / conflicting type definitions
No identical-shape duplicates found that could be auto-merged. All potential consolidations were either intentionally different subsets (e.g., `JOURNAL_PROMPTS` — 15 vs 5 prompts for different use cases) or flagged as D-holds. No changes committed; no STOP required.

**Commit:** `e901be2` — 3 missed duplicate default exports from Phase 2.

### Phase 4 — Consolidate duplicated logic / components
- No safe non-UI util de-duplications identified (all candidates were either intentionally different or UI-adjacent).
- `formatTime` functions: Intentionally different implementations for different display contexts — HOLD.
- All UI component merge proposals added to Deferred list (see below).

### Phase 5 — Backend sweep
**Commit:** `046b70a`  
Backend: 10 dead function exports removed, 7 dead type exports/re-exports removed.

**Files changed:**
- `backend/lib/achievement-definitions.ts`: Deleted `AchievementKey` type.
- `backend/lib/challenge-tasks.ts`: Made `mapTaskRowToApi`, `toTaskType`, `buildTaskConfigFromInput` private; removed `export type { TaskConfig }` re-export.
- `backend/lib/daily-challenge-generator.ts`: Made `pickTemplateForDate` private.
- `backend/lib/daily-challenge-templates.ts`: Made `DailyChallengeTaskTemplate` private.
- `backend/lib/date-utils.ts`: Made `formatDateKeyInTimeZone`, `dateKeyFromDate`, `parseDateKey` private.
- `backend/lib/push-reminder.ts`: Made `COMEBACK_TEMPLATES` private.
- `backend/lib/sanitize-search.ts`: Made `escapePostgrestFilter` private.
- `backend/lib/strava-service.ts`: Made `refreshAccessToken` private.
- `backend/trpc/guards.ts`: Made `ActiveChallengeRow` private.
- `backend/types/db.ts`: Made `ChallengeTaskRow`, `ParticipationType`, `RunStatus` private.

Backend test suites confirmed present in `npm test` (7 suites, 91 tests total).

### Phase 6 — Dependency hygiene
**Commit:** `39a2211`  
Removed `depcheck` and `ts-prune` from `devDependencies`. Both were installed but never referenced in any scripts or source files. Gated behind `npx expo export --platform ios` (exit 0, 12 MB bundle).

### Phase 7 — Final verification
- `components/home/StreakHeroV4.tsx`: Fixed pre-existing lint error (`'` → `&apos;`).
- `components/TaskEditorModal.styles.ts`: Deleted orphaned styles file (left behind when modal was converted to type-only in Phase 2).
- `lib/uploadProofImage.ts`: Deleted `uploadProofImageFromUri` — zero callers across entire codebase; `uploadProofImageFromBase64` is the live path (comment in file noted this was the fallback).
- All gates:
  - `tsc --noEmit` → exit 0
  - `npm test` → 16/16 files, 91/91 tests
  - `npm run lint` → exit 0
  - `npx expo export --platform ios` → exit 0, 12 MB bundle

---

## Deferred / Needs-Decision List

| ID | Item | Location | Reason Held |
|----|------|----------|-------------|
| D-1 | `app/create/index.tsx` + `CreateChallengeWizard` chain | `app/create/index.tsx`, `components/create/CreateChallengeWizard.tsx` | Live route; deprecation pending device verification — knip flags it as using a legacy component but it's an active Expo Router entry point |
| D-2 | `initializeRevenueCat` / `syncSubscriptionToSupabase` | `lib/subscription.ts` | RevenueCat identifiers — irreversible class, protected by hard rule |
| D-3 | `trackNotificationSent` | `lib/analytics.ts:321` | Analytics event definition — protected by hard rule |
| D-4 | `expo-system-ui` | `app.json` | Listed in app.json but not package.json — Expo peer dep, knip artifact, not actionable |
| D-5 | `babel-preset-expo` unresolved import | `babel.config.js` | Knip artifact — babel.config.js requires this at build time, not module resolution |
| D-6 | `docx` unlisted dep | `scripts/audit/build_scorecard.mjs:28` | Missing dep (not unused) — audit script uses it, needs to be added to package.json or script updated |
| D-7 | `TeamWithMembers` | `types/index.ts:170` | Developer annotation: "Q4 2026 team challenges" — intentionally kept |
| D-8 | `JOURNAL_PROMPTS` dual definition | `lib/task-helpers.ts` (5 prompts) vs `components/create/` (15 prompts) | Intentionally different subsets for different use cases |
| D-9 | `OBV2_SPACE` | `components/onboarding/v2/theme.ts:57` | Feature-flag-gated (`ONBOARDING_V2`) — treat as live |
| D-10 | `CreateFlowCheckbox` | `components/ui/CreateFlowCheckbox.tsx` | UI component, zero external consumers — needs device check before deletion |
| D-11 | `CreateFlowHeader` | `components/ui/CreateFlowHeader.tsx` | UI component, zero external consumers — needs device check before deletion |
| D-12 | `CreateFlowInput` | `components/ui/CreateFlowInput.tsx` | UI component, zero external consumers — needs device check before deletion |
| D-13 | `DurationPill` (ui version) | `components/ui/DurationPill.tsx` | Duplicate of live `DurationPill` in `wizard-shared.tsx`; the `ui/` version has zero consumers — needs device check before deletion |
| D-14 | `EnforcementBlock` | `components/ui/EnforcementBlock.tsx` | UI component, zero external consumers — needs device check before deletion |
| D-15 | `TaskTypeCard` (ui version) | `components/ui/TaskTypeCard.tsx` | Distinct from live `NewTaskTypeCard` in `NewTaskModal.tsx`; zero consumers — needs device check before deletion |
| D-16 | `knip.json` configuration hints | `knip.json` | 20 "Remove redundant entry/ignore" hints — cosmetic cleanup only, no code impact |

---

## Manual Pre-Merge Checklist (Yaseen)

Before merging `chore/dead-code-sweep-20260621` → `main`:

- [ ] **Device smoke test — iOS:**
  - [ ] App launches without crash
  - [ ] Home screen renders (StreakHeroV4, task cards, headers)
  - [ ] Challenge create flow works end-to-end (both `/create` route and `/(tabs)/create`)
  - [ ] Photo proof upload works (camera → upload → success)
  - [ ] Settings screen renders (AccountDangerZone, ReminderSection)
  - [ ] Share card renders correctly
  - [ ] Onboarding V2 flow (if `ONBOARDING_V2` flag is enabled)

- [ ] **Device smoke test — Android** (if applicable)

- [ ] **Backend sanity:**
  - [ ] Deploy backend and confirm no runtime errors on startup
  - [ ] Strava webhook handler works (strava-service.ts had `refreshAccessToken` made private)

- [ ] **Review D-10 through D-15** (UI components with zero consumers):
  - After confirming they're not rendered anywhere on device, approve deletion in a follow-up PR.

- [ ] **Review D-6** (`docx` missing dep in audit script):
  - Either `npm install docx --save-dev` or remove the audit script if no longer needed.

---

## Stats by Phase

| Phase | Files deleted | Exports removed | Lines deleted |
|-------|--------------|-----------------|---------------|
| 1 | 35 | — | ~4,200 |
| 2 | 6 | ~155 | ~4,100 |
| 3 | 0 | 0 | 0 |
| 4 | 0 | 0 | 0 |
| 5 | 2 | 17 | ~1,800 |
| 6 | 0 | 2 devDeps | 709 (package-lock) |
| 7 | 2 | 1 | ~55 |
| **Total** | **45** | **~173** | **~9,463** |
