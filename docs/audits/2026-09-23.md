# GRIIT static audit — 2026-09-23

HEAD audited: `1e257bf2016fcb2f0c0dfeddb457183796407f39` (`main`, handoff v35).
Job 1 build 61 submitted from this HEAD. Read-only. Ranked by severity inside each section.

---

## 1. Gates

| Check | Result | Severity |
|---|---|---|
| `tsc --noEmit` | 0 errors | — |
| `npm test` | **983 passed / 178 files**. No `it.skip` / `describe.skip` / `test.skip` in `lib/`, `backend/`, `tests/`. | — |
| `npx expo-doctor` | **17/18**. Fail: React Native Directory — `expo-live-activity` **Unmaintained**. | Medium |
| `npx expo install --check` | Dependencies up to date | — |
| `origin/main...main` | `0 0` | — |
| tree | clean | — |
| `git check-ignore ios/` | `.gitignore:57` | — |
| health | `200` `{"ok":true,"commit":"1e257bf"}` | — |

---

## 2. Feature flags (currently `false`)

Source: `lib/feature-flags.ts`. Gated code compiles (`tsc` 0).

| Flag | Line | Gates | Wired? | Tests | Severity |
|---|---|---|---|---|---|
| `PREMIUM_INTEGRATIONS` | `:20` | Strava / Health / WHOOP in settings (comment `:9–10`) | **Orphan** — no `FLAGS.PREMIUM_INTEGRATIONS` reads. Backend Strava live in `backend/trpc/routes/integrations.ts:25–47`. | None | Medium |
| `RUN_GOAL_CONFIG` | `:34` | Run goal type / target / tracking in Add-task | **Orphan**. Create wizard does not read it. Backend accepts `trackingMode` in `challenges-create.ts:81,386`. | None | Medium |
| `REAL_VERIFICATION` | `:43` | Home celebration waits for server `verified` | **Orphan at runtime.** Only asserted `tests/flows/task-flow.test.ts:20–21`. | Flag-exists only | High (honesty) |
| `FREEZE_SERVER_ENFORCED` | `:51` | Jeopardy “Use a freeze” CTA | **Wired** `components/home/JeopardyModal.tsx:149–184`. Home still calls freeze at `app/(tabs)/index.tsx:372–374` without the flag. | None | High |
| `JOURNAL_TAGS` | `:56` | Mood / Wins / Photo chips | **Orphan**. No `showTagChips`. | None | Low |
| `WORKOUT_STRUCTURED` | `:67` | Sets-and-reps workout | **Orphan**. LogStep is simple/timer. | None | Low |

`ONBOARDING_V2` and `COMPLETION_REWARDS` are **gone** from the flag file. Status doc is stale.

---

## 3. Palette leaks (`DS_COLORS` / `DS_DAYLIGHT` / `DS_COLORS_V2`)

Audit glob: `app components hooks lib` excluding tests. **FailedStep is the known exception.**

**Definitions (not leaks):** `lib/design-system.ts:8` `DS_COLORS`, `:1027` `DS_COLORS_V2`, `:1375` `DS_DAYLIGHT`.

### High — mixed on live V3 surfaces

| File | Notes |
|---|---|
| `components/LiveFeedSection.tsx:23,83,587,660,725–754,763` | `DS_COLORS` + `DS_DAYLIGHT` + `DS_V3` |
| `components/settings/AccountDangerZone.tsx:5,81–146` | already imports `DS_V3`; modal still `DS_COLORS` |
| `app/(tabs)/_layout.tsx:6,59,120–142` | `DS_DAYLIGHT` scene + `DS_COLORS` tab chrome |
| `components/home/JeopardyModal.tsx` | daylight leftovers |
| `components/ds/WeekStrip.tsx` | daylight leftovers |
| `lib/home-secured-visuals.ts:1,18` | `DS_COLORS_V2.brand.primary` mixed with `DS_V3` |

### Medium — screens / chrome still on v1 or v2

**app:** `+not-found.tsx`, `accountability.tsx`, `accountability/add.tsx`, `create-profile.tsx`, `_layout.tsx:27,110,410–419`, `(tabs)/teams.tsx`, `paywall.tsx`, `legal/privacy-policy.tsx`, `legal/terms.tsx`, `discover/category/[slug].tsx`, `auth/signup.tsx`.

**components:** `ErrorBoundary.tsx`, `ErrorRetry.tsx`, `InlineError.tsx`, `OfflineBanner.tsx`, `AuthGateModal.tsx`, `ProofShareCard.tsx`, `paywall/PaywallSocialProof.tsx`, `paywall/PaywallControl.tsx`, `ui/{PrimaryButton,EmptyState,CreateFlowInput,CreateFlowHeader,CreateFlowCheckbox,DurationPill,TaskTypeCard,GRIITWordmark,EnforcementBlock,InitialCircle}.tsx`, `shared/{ConfirmDialog,ImageViewerModal,FormInput,Avatar}.tsx`, `skeletons/{SkeletonBase,SkeletonFeedCard,SkeletonChallengeDetail,SkeletonLeaderboardRow,SkeletonProfile}.tsx`, `challenges/{CompactChallengeRow,_card-helpers}.tsx`, `feed/{FeedCardHeader,FeedEngagementRow,MilestonePostCard}.tsx`, `home/{StreakHeroV4,StreakFlame}.tsx`, `task-v2/{TaskFlowV2,TaskChrome}.tsx`, `task-v2/steps/ReviewStep.tsx`, `onboarding/v2/{theme.ts,ui.tsx,screens/ProfileScreen.tsx}`, `settings/settings-styles.ts`, `discover/CategoryChips.tsx`, `discover/grid/ChallengeGridCard.tsx`.

**lib:** `utils.ts` (avatar colors), `theme-palettes.ts`, `live-activity.ts`, `profile-v2-tokens.ts`, `notifications.ts:626`.

**Known exception:** `components/task-v2/steps/FailedStep.tsx:3,27` (`DS_COLORS_V2.semantic.dangerInk`).

**hooks:** none.

---

## 4. `"!"` in strings and the `"User"` fallback

### Exclamation marks (user-facing)

| File:line | String | Severity |
|---|---|---|
| `components/LiveFeedSection.tsx:285` | `` `${handle} is on Day … on GRIIT! 💪` `` | High (law 11: no `!`, no emoji) |
| `lib/notification-copy.ts:55` | `"🔥 {streak}-day streak!"` | High |
| `lib/notification-copy.ts:78` | `"⚠️ Last chance"` | Medium (emoji) |

Most other `!` hits are JS negation / `!==`, not copy.

### Literal `"User"` fallback

| File:line | Severity |
|---|---|
| `app/accountability/add.tsx:117` | High — `display_name \|\| username \|\| "User"` |
| `app/accountability.tsx:169` | High |
| `app/accountability.tsx:207` | High |
| `backend/trpc/routes/user.ts:44` | High — `(displayName \|\| "User")` written to profile |
| `backend/trpc/routes/challenges-create.ts:184` | High — `display_name: "User"` |
| `components/ds/Avatar.tsx:81` | Medium — `accessibilityLabel="User"` |

`lib/profile-display.ts:20–33` already implements display → username → first name and forbids the literal. Home greeting is clean.

`components/LiveFeedSection.tsx:295` `"User did not share"` is a share-sheet abort check, not a name fallback.

---

## 5. Chunk U contradictions 64–78

From `design/handoff/cursor/02_screens.md:4437–4724`.

| # | Spec | Codebase |
|---|---|---|
| **64** | Share/Keep needs `shared \| kept \| unanswered` | **Boolean.** `backend/trpc/routes/checkins.ts:139,832`; `backend/lib/activity-share.ts:7–8`; `backend/lib/proof-predicate.ts:197`; `lib/proofs-grid.ts:41,150–151`. Three-valued store: **not present**. |
| **65** | Denominator sentence on-screen | Partial: `lib/consistency.ts:29–39`. Spec sentence “Every day since you joined … not counting today”: **not present**. Visitor: `app/profile/[username].tsx:289–293`. |
| **66** | One `DayState` superset | **Split.** `lib/week-strip-days.ts:1` vs `backend/lib/record-days.ts:10–18`. Single union: **not present**. |
| **67** | Headline + grid from one `DayState[]` | Shared record payload, split metrics. Streak from bootstrap stats `app/(tabs)/profile.tsx:186–187` vs record consistency `:193`. |
| **68** | Visitor counts from shared photos only | **All camera tiles.** `backend/lib/proof-predicate.ts:149–198`; visitor `app/profile/[username].tsx:247,303`. Day-level drop: **not present**. |
| **69** | One card family, interpolated sentences | Sentence headlines: `lib/feed-copy.ts:20–34`; `components/feed/FeedPostV3.tsx:66–69,111–118`. Five-variant family: **not present**. |
| **70** | Remove Global leaderboard tab | **Global still default.** `components/activity/LeaderboardTab.tsx:137–138,167–171,345`. Per-challenge opt-in: **not present**. |
| **71** | Kill “check ins · days · pts” | **Still rendered.** `LeaderboardTab.tsx:27–30,330–332`; `lib/scoring.ts:5–6`. |
| **72** | One glyph table | **Two.** `components/ds/WeekStrip.tsx:87–97` + `lib/week-strip-days.ts` vs `backend/lib/record-days.ts:59–69` + `app/profile/consistency.tsx:71–72`. |
| **73** | Task subject validated against challenge | **Metadata trust.** `backend/lib/feed-activity-hydrate.ts:221` (`md.task_name`). |
| **74** | Authored day / secured claims | Derived with metadata fallback: `feed-activity-hydrate.ts:58–73,206`; `FeedPostV3.tsx:113–115`. |
| **75** | Streak + consistency from one reduction | **Multiple sources.** Home `app/(tabs)/index.tsx:265,588`; Profile `profile.tsx:186,193,220–222`. |
| **76** | `FeedEngagementRow` on daylight | Dead path `FeedEngagementRow.tsx:5,59,78`. Live feed uses V3 `ActionRow` `FeedPostV3.tsx:157,186–197`. |
| **77** | Hide counts at zero | V3 still hides: `FeedPostV3.tsx:189–191`. Dead path `FeedEngagementRow.tsx:64,79`. |
| **78** | Comments: some cards route, not sheet | Live feed → sheet `LiveFeedSection.tsx:427–428,700–702`. Inline compose in dead `FeedPostCard.tsx:252,278–299`. |

---

## 6. Rendered day / streak / secured / “of N”

**Allowed sources:** `lib/home-day-total.ts`, `lib/challenge-day.ts`, backend stats fields.

### Flagged (other source)

| Location | Shown | Source | Severity |
|---|---|---|---|
| `components/home/JeopardyModal.tsx:118` | `Day ${streak + 1}` | Client `streak+1` | High |
| `components/home/StreakHeroV4.tsx:454` | `Secure Day ${(streak ?? 0) + 1}` | Client math (dead UI) | Medium |
| `lib/challenge-end.ts:191` | `ended_on_day` | Raw `current_day` | High |
| `lib/challenge-end.ts:27–42` + `ChallengeEnd.tsx:126–169` | secured / longest streak | Client `securedCount` / `longestSecuredStreak` | High |
| `lib/secured-day.ts:62–63` | `Day n of length` | `Math.max` on params | Medium |
| `components/home/HomeV3.tsx:278,310` | `{done} / {total}` | `lib/home-proof-card.ts` task rollup | Medium |
| `components/home/TodayCard.tsx:85` | `{done} / {total}` | Onboarding demo | Low |
| `components/onboarding/v2/screens/DayTargetScreen.tsx:86` | `Day 1 of ${target}` | `targetStreak` | Low (product) |
| `app/challenge/complete.tsx:16–38` | streak | Route params | Medium |
| `app/(tabs)/discover.tsx:60` | `{streak}-day streak` | suggested-person field | Low (backend-ish) |

### Aligned (not flagged)

- Home streak + day line: `HomeV3.tsx:233,306` → stats + `homeProofDayLine` / `calendarDay`.
- Task-complete URL: `app/(tabs)/index.tsx:431` → `displayDay`.
- Active challenge: `app/challenge/active/[activeChallengeId].tsx:204,386` → `calendarDay`.
- Feed: `FeedPostV3.tsx:115` → `feedPostDisplayDay`.
- Profile/consistency: `profiles.getRecord` / bootstrap stats.

---

## 7. Writes inside `protectedProcedure.query`

| Query | Write | File:line | Severity |
|---|---|---|---|
| `integrations.getStravaActivities` | `connected_accounts.update` token refresh | `backend/trpc/routes/integrations.ts:99–108` (query `:83+`) | High — write on read |
| `integrations.getStravaAthlete` | same | `integrations.ts:138–147` (query `:122+`) | High |

No other listed `.query` performs insert/update/upsert/delete. `home.bootstrap` (`home.ts:117`) only fans out reads (`:62–113`). `listUnseenEndings` (`challenges.ts:522–551`) is read-only. Standing rule “no writes on read” is violated only on the Strava token path.

---

## 8. Schema drift

Session-proved missing in production:

| Column | Code | Migration | Severity |
|---|---|---|---|
| `active_challenges.completed_at` | **No TS usage** (comment only `20260920220000_challenge_end.sql:103`) | Baseline `20260621000000_baseline_schema_core_tables.sql:171` | Low |
| `milestone_30_shared` / `milestone_75_shared` | **Live read/write** `backend/trpc/routes/checkins.ts:1515–1525` | Repo has `20260409000000_active_challenges_milestone_shared.sql` **and** baseline `:173–174`. Session ruling: **absent in production**. | **Critical** |
| `check_ins.proof_photo_url` | **Not a DB column.** DTO/metadata: `lib/profile-v2-proof-photo.ts:5`; `backend/lib/proof-predicate.ts:59`; feed hydrate `feed-activity-hydrate.ts:208–230`; `feed.ts:205–209,412`. F1 dropped the column from the check_ins select. | **No migration** | Medium (name drift only) |

### Other backend columns vs latest defining migration

| Column | Notes | Severity |
|---|---|---|
| `active_challenges.started_at` | Fallback `app/(tabs)/index.tsx:218`, `app/challenge/active/[activeChallengeId].tsx:210`. No AC migration; `challenges.started_at` exists in `20250312000000_team_challenges.sql:13`. `select *` on AC is harmless if absent. | Medium |
| `ended_at` / `end_seen_at` | Defined `20260920220000_challenge_end.sql:8–10`. Present in code. | — |

---

## 9. Components no `app/` route reaches

**Critical — mounted never:** `components/home/StreakHeroV4.tsx:382` exported; type-imported `app/(tabs)/index.tsx:28`; Home renders `HomeV3` at `:585`. Dead hero.

**Not imported (directly or transitively) from `app/`:**

- `components/feed/{FeedPostCard,FeedCardHeader,MilestonePostCard,FeedEngagementRow,WhoRespectedSheet}.tsx`
- `components/create/NewTaskSheet.tsx`
- `components/AnalyticsBootstrap.tsx`, `AuthGateModal.tsx`, `shared/ImageViewerModal.tsx`
- Skeletons: `SkeletonChallengeDetail`, `SkeletonLeaderboardRow`, `SkeletonProfile`
- `components/ui` create-flow leftovers (`CreateFlowHeader`, `PrimaryButton`, `TaskTypeCard`, …)

`TodayCard.tsx` is reached only via onboarding `WhyProofScreen` → `app/onboarding/index.tsx`.

---

## 10. `TODO` / `FIXME` / `@ts-expect-error` / `@ts-ignore`

| File:line | Kind | Severity |
|---|---|---|
| `backend/lib/strava-callback.ts:80` | `@ts-expect-error` table type | Low — **leave alone** (Job 3) |
| `components/onboarding/v2/screens/SignInScreen.tsx:143` | `TODO` recovery link | Medium |
| `components/task-v2/TaskConfirmation.tsx:33` | `TODO(product)` completion trigger | Medium |
| `components/task-v2/MomentScreenV3.tsx:277` | `TODO(backend)` proofs for contact sheet | Medium |

No `@ts-ignore` / `FIXME` in `backend app components lib hooks` (non-test).

---

## 11. Launch blockers vs `claude/griit-status.md` (current evidence)

| Blocker | Status doc | Repo now | Severity |
|---|---|---|---|
| `app.json` `ios.usesAppleSignIn` | Missing | **`true`** `app.json:27`; plugin `:74` | Resolved in repo. Supabase Apple provider still **outside repo**. |
| RevenueCat / `"GRIIT Pro"` | Needs keys | Entitlement `lib/subscription.ts:16`; server `backend/trpc/routes/profiles.ts:26`. Missing key → early return `:59–64`. No committed `.env`. EAS secrets not verifiable from tree. | High (runtime) |
| `ONBOARDING_V2` | `false`, old flow live | **Flag gone.** V2 always mounted `app/onboarding/index.tsx:6`. | Status stale |
| Account deletion | Service-role risk | UI `AccountDangerZone.tsx:64–132` → `profiles.deleteAccount` `profiles.ts:520–535`. Auth user deleted only if `hasSupabaseAdmin()` `supabase-admin.ts:17–18`. Without service role: profile row gone, auth user remains. | High |
| Teams stub | Reachability | Screen `app/(tabs)/teams.tsx:12–28`; tab `href: null` `(tabs)/_layout.tsx:103–107`. Not in `lib/routes.ts`. File route may still deep-link. | Medium |
| PR #47/#48 debug logging | “revert later” | No `#47/#48` comments. `logger.debug` only if `LOG_LEVEL=debug` `backend/trpc/app-router.ts:11–59`. Client console `__DEV__` `lib/logger.ts:14`. | Low |

---

## 12. Branches

`git branch --no-merged main` count: **32**.

`chore/deep-clean-4`: **187 behind / 0 ahead** of `main`.
`perf/lists`: **194 behind / 0 ahead** of `main`.

Neither branch carries unique commits vs current main.

---

## 13. Chunk T on paper

```
app/_layout.tsx:381                <FinalizeEndedGate />
app/_layout.tsx:422–443            register; mount + AppState
lib/finalize-ended-foreground.ts:18  trpcMutate finalizeEnded
backend/trpc/routes/challenges.ts:504–509  mutation → applyFinalizeEnded
backend/lib/finalize-ended.ts:96–120     active_challenges.update; activity_events.insert
lib/finalize-ended-foreground.ts:20–22   shouldPresentEndScreen; listUnseenEndings; openEnd
lib/challenge-end.ts:199–204             path exclusions
app/_layout.tsx:438                      nav.push(CHALLENGE_END)
app/challenge/end.tsx:27–31              listUnseenEndings again
app/challenge/end.tsx:52–62              endedChallengeFromUnseen
app/challenge/end.tsx:75–84              markEndSeen (errors logged only)
backend/trpc/routes/challenges.ts:512–519 → applyMarkEndSeen
backend/lib/finalize-ended.ts:176–179    end_seen_at
```

### Failure → no end screen

| Step | Why | File:line |
|---|---|---|
| `finalizeEnded` throws | Caught; no navigation | `finalize-ended-foreground.ts:23–24` |
| `shouldPresentEndScreen` false | Skips even if unseen rows exist | `:20–22`; `challenge-end.ts:199–204` |
| E7 backfill `end_seen_at` | Historical actives never unseen | `20260920220000_challenge_end.sql:94–98` |
| Service role missing | Mutation 500, caught | `challenges.ts:505–508` |
| Empty unseen after load | Auto-replace home | `end.tsx:87–90,101–106` |
| `inFlight` | Concurrent foreground dropped | `finalize-ended-foreground.ts:15–16` |

### Failure → repeated end screen

| Step | Why | File:line |
|---|---|---|
| `markEndSeen` fails | Still `goHome`; `end_seen_at` unset | `end.tsx:79–84` |

Event insert failure (non-23505) finalizes enrollment but may omit feed event (`finalize-ended.ts:122–139`); end UI can still show.

---

## Top 10 (for morning report)

1. **Critical — schema:** `checkins.ts:1515–1525` reads/writes `milestone_30_shared` / `milestone_75_shared` that production lacks.
2. **Critical — dead hero:** `StreakHeroV4` never mounted; Home is `HomeV3`.
3. **High — Chunk T:** `finalizeEnded` catch or pathname gate → no end screen; `markEndSeen` fail → repeat.
4. **High — write on read:** Strava token `update` inside `getStravaActivities` / `getStravaAthlete` queries.
5. **High — contradiction 64:** share is still a boolean; day-viewer Share/Keep cannot distinguish dismissed vs declined.
6. **High — contradiction 70/71:** Global leaderboard + “check ins · days · pts” still shipping.
7. **High — day numbers:** `JeopardyModal` `streak+1` and end-screen counts are client math, not `home-day-total` / `challenge-day`.
8. **High — `"User"` writes:** `user.ts:44` and `challenges-create.ts:184` persist the literal.
9. **High — honesty flag:** `REAL_VERIFICATION` is false and unwired; `FREEZE_SERVER_ENFORCED` hides CTA but Home still mutates.
10. **Medium — palette:** LiveFeed / tab layout / AccountDangerZone still leak `DS_COLORS` / `DS_DAYLIGHT` onto V3 surfaces.
